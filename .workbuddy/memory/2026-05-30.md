# 2026-05-30 工作日志

## Day 8: 自研 ImageCompressor + 缩略图缓存 — 完成

### 新增文件
- `util/ImageCompressor.kt` — 自研下采样解码器
  - `decodeSampledBitmap()`: inJustDecodeBounds → calculateSampleSize → 正式解码（2的幂次）
  - `calculateSampleSize()`: 从 GalleryPickerAdapter 提取并泛化
  - `compressToFile()`: JPEG 质量压缩落盘（供后续上传使用）
- `util/ThumbnailCache.kt` — 4MB LruCache 缩略图缓存
  - Key: URI.toString()
  - 容量: ~11 张 300×300 RGBA 缩略图
  - ImageGridAdapter 和 GalleryPickerAdapter 共用

### 重构文件
- `ImageGridAdapter.kt`
  - 构造函数接收 CoroutineScope（MainActivity/MvcTestActivity 传 lifecycleScope）
  - onBindViewHolder: 取消旧 Job → 查 ThumbnailCache → IO 协程下采样 → adapterPosition 校验 → setImageBitmap
  - updateData: DiffUtil 增量刷新替代 notifyDataSetChanged
  - onViewRecycled: 取消 Job + 清理 Drawable
- `GalleryPickerAdapter.kt`
  - 删除内联 loadThumbnail/calculateSampleSize（~50行）
  - 改用 ImageCompressor.decodeSampledBitmap + ThumbnailCache
  - 异步加载：Thread + post 回 UI
- `MainActivity.kt`: ImageGridAdapter() → ImageGridAdapter(lifecycleScope)
- `MvcTestActivity.kt`: 同上 + 补充 lifecycleScope import

### 编译验证
- BUILD SUCCESSFUL（Kotlin daemon 崩溃走 fallback 策略通过）
- 无 lint 警告

### 关键决策
- 不用 AsyncListDiffer（与 ItemTouchHelper 拖拽冲突），改用 DiffUtil.calculateDiff 手动分发
- 不用 absoluteAdapterPosition/bindingAdapterPosition（旧 RecyclerView 不支持），改用 adapterPosition
- updateData 时 ThumbnailCache.evictAll()，避免旧缩略图占空间

---

## Day 9: ViewPager2 大图预览 — 完成

### 新增文件
- `res/layout/activity_image_preview.xml` — 全屏预览布局
  - ViewPager2 主体 + 顶部 ✕ 关闭按钮（bg_circle_dark 圆形背景）
  - 底部页码指示器 "1/9"（半透明黑底白字）
- `res/layout/item_image_preview.xml` — 预览页单项：ImageView FIT_CENTER
- `res/drawable/bg_circle_dark.xml` — 关闭按钮圆形暗色背景
- `view/ImagePreviewActivity.kt` — 全屏大图预览 Activity
  - Intent 接收：ArrayList<String> URI 列表 + position 初始位置
  - ViewPager2 offscreenPageLimit=1 懒加载
  - ImageCompressor.decodeSampledBitmap(target=1080px) 预览分辨率
  - ThumbnailCache 缓存（key="preview_{uri}"）
  - CoroutineScope + SupervisorJob：Activity 销毁时取消所有加载任务
  - PreviewViewHolder：boundPosition 校验防止错图

### 修改文件
- `ImageGridAdapter.kt`: setListeners() 新增 onImageClickListener 参数（默认 {}）
  - bindImageHolder 中 imageView.click → invoke 回调
- `MainActivity.kt`: initRecyclerView 中传入 onImageClickListener → launchImagePreview()
  - 新建 launchImagePreview() 方法：构建 Intent 传 URI 列表 + 当前位置
  - 补充 import android.content.Intent
- `MvcTestActivity.kt`: 不受影响（onImageClickListener 有默认值）
- `AndroidManifest.xml`: 注册 ImagePreviewActivity（exported=false）

### 编译验证
- BUILD SUCCESSFUL（Kotlin daemon fallback 通过）
- 无 lint 警告

### 关键决策
- 预览目标尺寸 1080px（屏幕宽度典型值），9 张 4000×3000 原图 → 内存 ~1080×810×4×3（预加载）= ~10MB，安全
- ThumbnailCache 复用于预览（key 前缀 "preview_"），九宫格缩略图（300px）不冲突
- 关闭按钮用 ✕ 字符 + 暗色圆形背景，简洁不遮挡内容
- MvcTestActivity 无预览（不影响沙盒页）
